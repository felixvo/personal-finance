{
  "name": "atlas",
  "version": "0.0.0",
  "private": true,
  "description": "Project Atlas — a personal financial operating system (monorepo root).",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
